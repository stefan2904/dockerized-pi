# AGENTS.md

## GitHub CLI (very brief)
- Check login: `gh auth status`
- Setup auth for repo: `gh auth setup-git`
- Create and push repo from current dir: `gh repo create <owner>/<repo> --source=. --remote=origin --push`
